{
  "name": "used-car-intelligence",
  "version": "1.0.0",
  "private": true,
  "description": "Used Car Intelligence App Monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run start:dev --workspace=apps/api",
    "dev:admin": "npm run dev --workspace=apps/admin",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:mobile": "npm run start --workspace=apps/mobile",
    "build": "npm --prefix packages/shared run build && npm --prefix apps/api run build && npm --prefix apps/web run build",
    "build:api": "npm --prefix packages/shared run build && npm --prefix apps/api run build",
    "build:web": "npm --prefix packages/shared run build && npm --prefix apps/web run build",
    "postinstall": "npm run postinstall --workspace=apps/api || true",
    "db:validate": "npm run db:validate --workspace=apps/api",
    "db:migrate": "npm run db:migrate --workspace=apps/api",
    "db:migrate:prod": "npm run db:migrate:prod --workspace=apps/api",
    "db:seed": "npm run db:seed --workspace=apps/api"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
